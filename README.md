# Near Mobile App

Мобильное приложение "Рядом" на React Native - платформа для поиска исполнителей задач поблизости.

## Возможности

- 🗺️ Карта с задачами поблизости
- 📋 Список задач с фильтрацией
- ➕ Создание и редактирование задач
- 💬 Чат между заказчиком и исполнителем
- 👤 Профили пользователей с рейтингами
- ⭐ Система отзывов

## Технологии

- React Native
- Expo
- TypeScript
- Zustand (state management)
- Supabase (backend)
- React Navigation
- React Native Maps

## Установка

1. Установите зависимости:
```bash
npm install
```

2. Установите Expo CLI глобально (если еще не установлен):
```bash
npm install -g expo-cli
```

3. Запустите приложение:
```bash
npm start
```

4. Откройте приложение:
   - Нажмите `a` для Android эмулятора
   - Нажмите `i` для iOS симулятора
   - Отсканируйте QR код в Expo Go приложении на телефоне

## Настройка

### Supabase

Приложение использует Supabase для backend. Убедитесь, что:
- Supabase URL и ключ настроены в `src/lib/supabase.ts`
- База данных настроена согласно схеме из веб-версии

### Геокодинг

Для работы геокодинга необходимо:
1. Получить API ключ Yandex Geocoding API
2. Добавить ключ в `src/lib/geocoding.ts` (замените `YOUR_YANDEX_API_KEY`)

### Карты

Для работы карт на Android необходимо добавить Google Maps API ключ в `app.json`:
```json
{
  "expo": {
    "android": {
      "config": {
        "googleMaps": {
          "apiKey": "YOUR_GOOGLE_MAPS_API_KEY"
        }
      }
    }
  }
}
```

## Структура проекта

```
src/
├── components/       # Переиспользуемые компоненты
├── lib/             # Утилиты и конфигурация
├── navigation/       # Навигация приложения
├── screens/          # Экраны приложения
├── store/            # Zustand stores
└── types/            # TypeScript типы
```

## Разработка

### Добавление нового экрана

1. Создайте файл в `src/screens/`
2. Добавьте экран в навигацию (`src/navigation/AppNavigator.tsx`)

### Добавление нового store

1. Создайте файл в `src/store/`
2. Используйте Zustand для управления состоянием

## Сборка

### Android
```bash
expo build:android
```

### iOS
```bash
expo build:ios
```

## Лицензия

MIT
